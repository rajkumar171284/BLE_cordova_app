import { DirectiveIfDirective } from './directive-if.directive';

describe('DirectiveIfDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveIfDirective();
    expect(directive).toBeTruthy();
  });
});
